# Incluído no server block do EB — timeouts do proxy nginx para o Node.
# Sem isto, o default (~60s) corta geração longa e o browser mostra erro de CORS.
client_max_body_size 50M;
proxy_connect_timeout 360s;
proxy_send_timeout 360s;
proxy_read_timeout 360s;
send_timeout 360s;
